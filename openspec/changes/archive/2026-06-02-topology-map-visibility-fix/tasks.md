## 1. Fix SVG Gradient

- [x] 1.1 Add `<radialGradient id="center-glow">` to the SVG `<defs>` in `decision-topology-map.tsx`.
- [x] 1.2 Update the `<circle>` fill attribute to use `url(#center-glow)`.

## 2. Improve Text Visibility

- [x] 2.1 Add text outline/stroke to scope anchor `<text>` elements.
- [x] 2.2 Adjust floating node label background opacity for better contrast.
