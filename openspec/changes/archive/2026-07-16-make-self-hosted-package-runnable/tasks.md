## 1. Runnable Package Contents

- [x] 1.1 Add explicit root-file and runtime-tree allowlists to the package builder, copy the Node workspace, web/API runtime, Python engine and migrations, prompts, Compose support, and bounded smoke assets, and reject excluded secret/cache/generated paths.
- [x] 1.2 Extend the package manifest and generated README with runtime asset inventory, dependency-download boundary, install commands, startup command, smoke command, and independent/customer-host evidence boundary.

## 2. Fail-Closed Verification And Rehearsal

- [x] 2.1 Extend package verification so missing runtime metadata, lockfiles, application/engine source, Compose support, prompts, or smoke entry points are blocking while historical structure-only packages remain readable but not runnable proof.
- [x] 2.2 Extend clean-install rehearsal to check runnable entry points from its isolated package copy and preserve local, independent-runner, external, and customer-controlled proof levels.
- [x] 2.3 Add a bounded runnable-package rehearsal that installs exact dependencies and starts engine/API/web plus browser smoke from the package copy, and add a path-filtered GitHub-hosted Windows workflow that publishes sanitized evidence artifacts.

## 3. Tests And Real Validation

- [x] 3.1 Add deterministic tests for runtime inclusion, secret/cache exclusion, legacy package blocking, missing runtime assets, isolated-copy command roots, and independent-runner boundary classification.
- [x] 3.2 Run focused package tests, full engine/API/web tests, typecheck, benchmark fixture validation, guardrail, and OpenSpec strict validation.
- [x] 3.3 Build a fresh package, install and start it from an isolated directory outside the source tree, select a fresh real public GitHub repository, and complete real Chrome/browser/computer dashboard, review, Why, Drift, and evidence navigation without reading runtime files from the maintainer checkout.

## 4. Evidence And Closeout

- [x] 4.1 Generate package verification, clean install, runnable runtime, release/readiness, and independent-host evidence; archive a dated readiness entry and update the taskbook, dated update log, package docs, and next-development plan with honest customer-proof boundaries.
- [x] 4.2 Archive the OpenSpec change, commit only scoped files, push the dedicated branch, run and inspect both normal CI and the package rehearsal workflow, and report exact GitHub Actions results.
