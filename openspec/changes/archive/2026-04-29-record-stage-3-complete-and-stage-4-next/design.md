## Context

This is a documentation-only state correction after `productize-workspace-reuse-and-incremental-sync` was completed and pushed.

## Goals / Non-Goals

**Goals:**

- Keep the master plan aligned with the real repository and OpenSpec state.
- Make Stage 4 the clear next planning target.

**Non-Goals:**

- No product behavior changes.
- No API, data model, or UI changes.
- No spec requirement changes.

## Decisions

- Use a small OpenSpec change for traceability even though the implementation is documentation-only.
- Do not create delta specs because no capability requirement changes.

## Risks / Trade-offs

- Plan wording can drift again after the next implementation slice -> keep future stage transitions as explicit doc updates.
